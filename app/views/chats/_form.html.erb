<%= form_for(@chat) do |f| %>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <input type='button' name='btn-send' class='btn-send' id='btn-send' value='Отправить'>
  <!-- Позже эта кнопка превратится в кнопку поиска собеседника
   <input type='button' name='btn-get' class='btn-get' id='btn-get' value='получить'> -->
  
<% end %>
