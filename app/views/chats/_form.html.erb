
<%= form_for(@chat) do |f| %>
    <div id='message_box'>
      <%= f.text_area :message, :size => "", :class => 'neon' %>
    </div>
    <div id='btn-form'>
    	<div id='btn-wrapper'>
	    	<span class='btn-manage btn-send neon' id='btn-send'>Send</span>
	     	<span class='btn-manage btn-search noen' id='btn-search'>Search</span>
	     </div>
    </div>

<% end %>
